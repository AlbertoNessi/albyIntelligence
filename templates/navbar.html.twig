<div class="d-flex justify-content-center py-3">
    <div class="text-center">
        <h1 class="text-center fw-bold">
            <a href="/" style="text-decoration: none; color: black">Alby Intelligence</a>
        </h1>
    </div>
</div>

<a
    class="btn btn-dark mx-4 my-4 shadow"
    style="top: 0; left: 0; position: absolute"
    href="{{ path('showDocumentation_url') }}"
    role="button"
>
    <i class="bi bi-journal-code"></i> Gestisci documentazione
</a>

<div class="buttonContainerPosition">
    <a
        class="btn btn-dark mx-4 my-4 shadow"
        href="{{ path('showSemanticIndexContent_url') }}"
        role="button"
    >
        <i class="bi bi-eye-fill"></i> Mostra indice
    </a>
    <button class="btn btn-dark mx-4 my-4 shadow" id="updateIndexBtn">
        <span class="normal-state">
            <i class="bi bi-arrow-clockwise"></i> Aggiorna indice
        </span>
        <span class="loading-state d-none">
            <span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span>
            Loading...
        </span>
    </button>
</div>


<div id="lastUpdatedTime" class="mx-4 my-4 text-success" style="position: absolute; top: 50px; right: 0;">
    Ultimo aggiornamento: <small id="lastUpdatedTimestamp"></small>
</div>

<header class="text-center flex justify-around my-4">
    <a
        id="contacts"
        class="btn {% if table_id == 13 %} btn-dark shadow {% else %} btn-outline-dark {% endif %} menuItems"
        href="{{ path('list_url', {id: 13}) }}"
        role="button"
        aria-current="page"
    ><i class="bi bi-bag"></i> Ordini</a>

    <a
        id="contacts"
        class="btn {% if table_id == 14 %} btn-dark shadow {% else %} btn-outline-dark {% endif %} menuItems"
        href="{{ path('list_url', {id: 14}) }}"
        role="button"
        aria-current="page"
    ><i class="bi bi-people"></i> Clienti</a>

    <a
        id="contacts"
        class="btn {% if table_id == 1 %} btn-dark shadow {% else %} btn-outline-dark {% endif %} menuItems"
        href="{{ path('list_url', {id: 1}) }}"
        role="button"
        aria-current="page"
    ><i class="bi bi-person-vcard"></i> Contacts</a>

    <a
        id="emails"
        class="btn {% if table_id == 2 %} btn-dark shadow {% else %} btn-outline-dark {% endif %} menuItems"
        href="{{ path('list_url', {id: 2}) }}"
        role="button"
        aria-current="page"
    ><i class="bi bi-envelope"></i> Emails</a>

    <a
        id="messages"
        class="btn {% if table_id == 3 %} btn-dark shadow {% else %} btn-outline-dark {% endif %} menuItems"
        href="{{ path('list_url', {id: 3}) }}"
        role="button"
        aria-current="page"
    ><i class="bi bi-chat-dots"></i> Messages</a>

    <a
        id="notes"
        class="btn {% if table_id == 4 %} btn-dark shadow {% else %} btn-outline-dark {% endif %} menuItems"
        href="{{ path('list_url', {id: 4}) }}"
        role="button"
        aria-current="page"
    ><i class="bi bi-journal-text"></i> Notes</a>

    <a
        id="events"
        class="btn {% if table_id == 5 %} btn-dark shadow {% else %} btn-outline-dark {% endif %} menuItems"
        href="{{ path('list_url', {id: 5}) }}"
        role="button"
        aria-current="page"
    ><i class="bi bi-calendar-event"></i> Events</a>

    <a
        id="reminders"
        class="btn {% if table_id == 6 %} btn-dark shadow {% else %} btn-outline-dark {% endif %} menuItems"
        href="{{ path('list_url', {id: 6}) }}"
        role="button"
        aria-current="page"
    ><i class="bi bi-alarm"></i> Reminders</a>

    <a
        id="calendar"
        class="btn {% if table_id == 7 %} btn-dark shadow {% else %} btn-outline-dark {% endif %} menuItems"
        href="{{ path('list_url', {id: 7}) }}"
        role="button"
        aria-current="page"
    ><i class="bi bi-calendar"></i> Calendar</a>

    <a
        id="tasks"
        class="btn {% if table_id == 8 %} btn-dark shadow {% else %} btn-outline-dark {% endif %} menuItems"
        href="{{ path('list_url', {id: 8}) }}"
        role="button"
        aria-current="page"
    ><i class="bi bi-check2-square"></i> Tasks</a>

    {# <a
        id="notifications"
        class="btn {% if table_id == 9 %} btn-dark shadow {% else %} btn-outline-dark {% endif %} menuItems"
        href="{{ path('list_url', {id: 9}) }}"
        role="button"
        aria-current="page"
    ><i class="bi bi-bell"></i> Notifications</a> #}

    {# <a
        id="locations"
        class="btn {% if table_id == 10 %} btn-dark shadow {% else %} btn-outline-dark {% endif %} menuItems"
        href="{{ path('list_url', {id: 10}) }}"
        role="button"
        aria-current="page"
    ><i class="bi bi-geo-alt"></i> Locations</a> #}
</header>

